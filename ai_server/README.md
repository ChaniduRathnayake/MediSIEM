# CAAP IoMT IDS — ML Pipeline
**Clinically Aware Alert Prioritization for Smart Hospitals**  
R.M.C.B. Rathnayake | IT22061270 | SLIIT Cyber Security

---

## ⚡ Quick Start

```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Drop your datasets here (do NOT rename them):
#    data/train/iomt_train.csv
#    data/test/iomt_test.csv

# 3. Train all 3 models
python train.py

# 4. Evaluate on test set
python test.py

# 5. Start Flask AI server (for MERN dashboard)
python app.py
```

---

## Dataset Format

Your CSV files must have:

### 45 Network Feature Columns (ML input)
```
Header_Length, Protocol Type, Duration, Rate, Srate, Drate,
fin_flag_number, syn_flag_number, rst_flag_number, psh_flag_number,
ack_flag_number, ece_flag_number, cwr_flag_number,
ack_count, syn_count, fin_count, rst_count,
HTTP, HTTPS, DNS, Telnet, SMTP, SSH, IRC,
TCP, UDP, DHCP, ARP, ICMP, IGMP, IPv, LLC,
Tot sum, Min, Max, AVG, Std, Tot size,
IAT, Number, Magnitue, Radius, Covariance, Variance, Weight
```

### 1 Label Column
```
label   →  ARP_Spoofing | DoS_TCP | MQTT_Publish_Flood |
           MQTT_Brute_Force | Recon | Replay | Benign
```

### 6 Medical Metadata Columns (CAS scoring — not ML input)
| Column | Type | Values | CAAP Use |
|---|---|---|---|
| `device_type` | string | ICU Ventilator, Defibrillator, Infusion Pump, etc. | Context |
| `cc_score` | int | 1–10 | CC dimension (÷2 → 1–5) |
| `department` | string | ICU, Ward, Radiology, etc. | Context |
| `patient_dependency` | string | life_critical / high / medium / low / none | Context |
| `time_sensitivity` | int | 1–5 | TS base value |
| `shift` | string | day / evening / night | TC dimension |

---

## Project Structure
```
caap/
├── data/
│   ├── train/
│   │   └── iomt_train.csv      ← DROP YOUR FILE HERE
│   └── test/
│       └── iomt_test.csv       ← DROP YOUR FILE HERE
├── models/                      ← Auto-generated by train.py
│   ├── random_forest.pkl
│   ├── isolation_forest.pkl
│   ├── kmeans.pkl
│   ├── scaler.pkl
│   ├── label_encoder.pkl
│   └── feature_cols.pkl
├── reports/                     ← Auto-generated by train.py
│   ├── confusion_matrix.png
│   ├── per_class_accuracy.png
│   ├── class_distribution.png
│   ├── feature_importances.png
│   ├── if_anomaly_dist.png
│   ├── kmeans_clusters.png
│   ├── cas_distribution.png
│   ├── model_summary.png
│   ├── classification_report.txt
│   └── predictions.csv
├── src/
│   └── cas_engine.py            ← CAS 5-dimension scoring engine
├── train.py                     ← Run this first
├── test.py                      ← Run after train.py
├── app.py                       ← Flask server for MERN dashboard
└── requirements.txt
```

---

## 3-Model Architecture

| Model | Config | CAAP Role | Output |
|---|---|---|---|
| **Random Forest** | 300 trees, balanced weights, OOB | TR dimension — attack classification | label + confidence |
| **Isolation Forest** | 200 trees, contamination=0.10 | TS dimension — anomaly detection | anomaly score + flag |
| **K-Means** | k=3, k-means++, 20 init | Traffic context | active / routine / idle |

---

## CAS Formula

```
CAS = 0.25·TR + 0.30·CC + 0.25·TS + 0.10·AE + 0.10·TC
CAS is scaled ×2 to produce a 0–10 score (matches CVSS convention)
```

| Dimension | Weight | Source | Scale |
|---|---|---|---|
| TR — Threat Risk | 0.25 | RF `predict_proba` max | 1–5 |
| CC — Clinical Criticality | 0.30 | `cc_score` ÷ 2 | 1–5 |
| TS — Time Sensitivity | 0.25 | IF anomaly score + `time_sensitivity` col | 1–5 |
| AE — Active Exploitation | 0.10 | Attack type CVE lookup | 0–5 |
| TC — Temporal Context | 0.10 | `shift` column | 1–5 |

**Action thresholds:**
- CAS ≥ 8.0 → **Immediate**
- CAS ≥ 5.0 → **Investigate**
- CAS < 5.0 → **Monitor**

---

## Flask API — POST /predict

**Request** (JSON — all 45 network features + optional medical):
```json
{
  "Header_Length": -0.094,
  "Protocol Type": 1.42,
  "Duration": -0.088,
  "Rate": -0.38,
  "TCP": -0.85,
  "ARP": 0.80,
  "IAT": 4.73,
  "Magnitue": 7.65,
  "... (all 45 features) ...": "...",
  "cc_score": 10,
  "shift": "night",
  "time_sensitivity": 5
}
```

**Response:**
```json
{
  "label": "DoS_TCP",
  "confidence": 0.9833,
  "TR_score": -0.42,
  "cluster": "active",
  "is_anomaly": true,
  "CAS": 9.60,
  "action": "Immediate",
  "dimensions": {
    "TR": 5.0,
    "CC": 5.0,
    "TS": 5.0,
    "AE": 5.0,
    "TC": 5.0
  }
}
```

**Other endpoints:**
- `GET /health` — status, class list, feature list
- `GET /features` — full input/output schema

---

## PP1 Viva Quick Reference

| Question | Answer |
|---|---|
| Why 3 models? | Each serves a different CAS dimension: RF→TR, IF→TS, KMeans→context. No single model can do all three. |
| Why class_weight="balanced"? | Handles minority attack classes (Replay, Recon) without SMOTE — RF natively rebalances. |
| Why OOB score? | Free validation estimate from bootstrap samples — confirms no overfitting before test. |
| Why contamination=0.10? | ~10% of hospital IoMT traffic expected anomalous — aligns with CIC IoMT 2024 distribution. |
| Why shift → TC? | Night shift = fewer staff = slower response time = higher clinical risk. |
| Why cc_score ÷ 2? | Dataset uses 1–10 scale; CAS formula uses 1–5. Simple normalisation preserves ordering. |
