# Engine dependencies
# Install with:  pip install -r requirements.txt

# Web framework + ASGI server
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Schema validation (the powerhouse for our Pydantic models)
pydantic==2.9.2

# Testing
pytest==8.3.3
httpx==0.27.2          # Used by FastAPI's TestClient for endpoint tests
